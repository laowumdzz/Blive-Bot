- 消息处理器通过 @Handler.append_func 装饰器注册到 live_streams 的消息分发器，而非直接调用客户端回调
- 配置统一通过环境变量读取（LIVE_ROOM_ID、LOG_PATH），config.toml 仅作为模板存在
- 异步任务以 asyncio.Event().wait() 保持事件循环运行，异常通过 CancelledError 捕获并优雅关闭资源
- 日志通过 loguru 双输出（文件按天轮转 + stdout），DEBUG 级别写入文件，INFO 级别输出到控制台